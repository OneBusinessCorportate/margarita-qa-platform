# Журнал изменений

Запись формата «что / зачем / результат» обязательна для каждого PR, меняющего код: без неё робот `doc-sync` краснит и мердж заблокирован. Форточка для заведомо тривиальной правки — метка PR `skip-changelog`.

## 2026-07-17 (5)
- Отдельный трекер «Системные задачи бухгалтеров» (п.6).
  - **Что.** Новый раздел `/system-tasks` («Системные задачи») — отдельный список задач бухгалтеров, НЕ смешанный с апелляциями и с общими задачами по чатам. По каждой задаче: бухгалтер, клиент/чат, связанный QA-тикет, название, описание, приоритет (Low/Medium/High), статус (`new`/`in_progress`/`postponed`/`completed`/`cancelled`), Due Date Original, Due Date Postponed, Completed At, Created By, Created At. Создание, смена статуса/приоритета, перенос срока — inline; при переходе в `completed` проставляется `completed_at`, при выходе — снимается. Сводка сверху (всего/открытых + по статусам) и фильтры по бухгалтеру/статусу.
  - **Как.** Таблица `mqa_accountant_system_tasks` (миграция `20260717_mqa_accountant_system_tasks.sql` + `db/schema.sql`), мягкая связь `ticket_id → mqa_violations(id) on delete set null`. Чистые правила в `src/lib/system-tasks.ts` (статусы/приоритеты/лейблы, `validateTitle`, `normalizeSystemTaskPatch`, `visibleSystemTasks`, `summarizeSystemTasks`) — покрыты юнит-тестами. Репозиторий: `listAccountantSystemTasks`/`createAccountantSystemTask`/`updateAccountantSystemTask` (Supabase + in-memory). API: `GET/POST /api/system-tasks`, `PATCH /api/system-tasks/[id]`; `dbErrorResponse` обобщён на доменные ошибки с `httpStatus` (`SystemTaskError`). UI: `SystemTasksPanel` + страница + пункт меню.
  - **Зачем.** Пункт 6 ТЗ: задачи по QA-тикетам должны вести отдельным трекером, не путая с апелляциями. Видимость: этот бэк-офис — Маргарита/руководство видят все задачи (фильтр по бухгалтеру); персональная видимость «только свои» — на стороне приложения бухгалтеров (правило `visibleSystemTasks` переиспользуемо там же).
  - **Результат.** `npm test` — 472 зелёные (добавлен `tests/system-tasks.test.ts`, +10), `npm run build` зелёный (роуты `/system-tasks`, `/api/system-tasks`, `/api/system-tasks/[id]` зарегистрированы), `next lint` без новых ошибок. Неразрушающая миграция; существующие данные не трогаются. Апелляции и системные задачи разведены (разные таблицы, разные разделы).

## 2026-07-17 (4)
- Ежедневный Telegram-отчёт «Апелляции и QA Маргариты» — автоматический + ручной запуск, с поддержкой отдельного чата.
  - **Что.** Раздел работы Маргариты (проверено чатов, создано тикетов, ознакомлено бухгалтерами, подано/подтверждено/отклонено апелляций, ожидают решения, не обработано бухгалтерами + разбивка по каждому бухгалтеру) теперь уходит в Telegram сам по расписанию и может быть отправлен вручную для проверки. Данные — только QA Маргариты (`getViolationWorkflowReport` → `buildViolationWorkflowReport` по `mqa_violations`/`mqa_violation_appeals`), без ИИ и без Excel; та же агрегация, что на `/work-report` и `/dashboard`, поэтому цифры совпадают. Формат блока приведён к запрошенному тексту («Апелляции и QA Маргариты», «Создано тикетов», «Подтверждено/Отклонено Маргаритой», построчная разбивка `- тикетов/ознакомлен/апелляций/подтверждено/отклонено/pending`).
  - **Как.** Общий сборщик `src/lib/margarita-report.ts` (без `server-only`): `assembleMargaritaReport` (окно по умолчанию — сегодня по Еревану, поддержаны `--date`/`--from`/`--to`/`--accountant`), `pickMargaritaChatId` (предпочитает `MARGARITA_QA_TELEGRAM_CHAT_ID`, иначе `TELEGRAM_CHAT_ID`), `resolveMargaritaWindow`, `yerevanToday`. Сырые сендеры вынесены в `src/lib/telegram-core.ts` (без `server-only`), чтобы их разделяли и Next-роуты (через `telegram.ts` с прежним API), и cron-скрипт. Автоотправка — cron-сервис `margarita-qa-report` (Render, `render.yaml`) запускает `npm run report:margarita` → `scripts/send-margarita-report.ts` (сам, без разбуживания веб-сервиса; падает громко, если Supabase не настроен, чтобы не разослать ложные нули). Ручной запуск — новый роут `GET/POST /api/telegram/margarita-report` (GET — превью без отправки; POST — отправка в чат Маргариты) и кнопка «Отправить в Telegram» в разделе «Сообщения» (`chat="margarita"` в `SendTelegramButton` → `/api/telegram/send` резолвит отдельный чат).
  - **Зачем.** Нужен был именно ежедневный Telegram-отчёт по процессу апелляций и QA Маргариты, автоматический и с ручной проверкой; при этом без лишних переменных окружения — текущих `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` достаточно, а `MARGARITA_QA_TELEGRAM_CHAT_ID` добавляется опционально только если отчёт нужен в отдельном чате.
  - **Результат.** `npm test` — 462 теста зелёные (добавлен `tests/margarita-report.test.ts`; обновлены `telegram-margarita`/`accountant-integration` под новый формат), `npm run build` зелёный (роут `/api/telegram/margarita-report` зарегистрирован), `next lint` без новых ошибок. Cron-скрипт проверен `--dry-run` (модульный граф грузится вне Next, guard по Supabase срабатывает). Существующая логика ознакомления/апелляций/ревью Маргаритой не менялась — только добавлена рассылка и приведён формат. Env: см. итог ниже.

## 2026-07-17 (3)
- Честная уверенность модели + калибровка по правкам Маргариты и починка автоопределения рассылок по долгам.
  - **Уверенность стала честной (не 90–96 у всех).** Что: `predictionConfidence` считалась от ОБЪЁМА истории (`45 + до 52 за число оценок − bias`), поэтому у любого бухгалтера с историей насыщалась к 90–97%, а «gated»-строка была фиксированные 95% — значение отражало «сколько у нас данных», а не «насколько уверена модель в ЭТОЙ оценке». Теперь уверенность собирается из доказательности конкретного прогноза и стартует НИЗКО: определён ли бухгалтер, сколько реальной истории по нему, насколько СОГЛАСОВАНЫ его прошлые оценки (дисперсия по `sumSq`), полнота фактов по статусам рассылок, штраф за историческую ошибку модели; «gated» — высокая, но масштабируется полнотой фактов, а не константой. Значения >90% теперь редки и заслужены (`src/lib/ai.ts`). Добавлены сырое значение, факторы и краткие evidence-пояснения (`uncertainty[]`, без chain-of-thought). Зачем: убрать иллюзию «модель почти всегда права». 
  - **Слой калибровки — реальный feedback по правкам Маргариты (не «дообучение» внешней модели).** Что: новый `src/lib/confidence-calibration.ts` строит по историческим оценкам таблицу «бакет уверенности → наблюдаемая точность (accepted/reviewed)» и отображает сырую уверенность на неё (усадка по объёму данных). Консервативный fallback: при недостатке данных возвращаем сырое значение (НЕ завышаем) и помечаем «предварительно». Без утечки: калибровка строится только по прошлым строкам (есть опция `before`), исходная замороженная `ai_confidence` не использует будущее.
  - **Определение match / partial / mismatch.** Что: единая чистая функция `src/lib/match.ts` (`classifyMatch`, толеранс `SCORE_TOLERANCE = 5` на нормированной шкале 0..100): exact — ничего важного не изменилось; partial — та же категория/решение о рассылке, балл в пределах ±5 или правка критерия; mismatch — сменилась категория/решение или |Δ| > 5. Исходная AI-оценка НЕ перезаписывается (было сделано ранее), строки без AI-снимка исключаются из статистики.
  - **Отчёт «Уверенность модели» расширен** (`src/lib/confidence-report.ts`, `src/components/ConfidenceReport.tsx`, API): exact/partial/mismatch и их проценты, средняя/медианная разница баллов, средняя уверенность совпавших/несовпавших, разбивка по бакетам (совпадения + средняя Δ), статистика по бухгалтерам, детальная таблица исправленных оценок, ВТОРАЯ корреляция (уверенность ↔ |Δ баллов|), число исключённых без AI-снимка. Новые фильтры: чат/клиент и статус совпадения. Корреляция подписана как точечно-бисериальная, «корреляция ≠ причинность», предупреждение о нехватке данных сохранено.
  - **UI уверенности честнее** (`ScoringPanel`): ярлык (Низкая/Средняя/Высокая/Очень высокая), предупреждающий стиль при низкой уверенности/неполных данных/предварительной калибровке (не «всё зелёное»), «Недостаточно данных» вместо подмены на 90%.
  - **Рассылки по долгам снова подтягиваются (общая причина, не один чат).** Что: лента сообщений хранит супергруппу под Bot-API-id `-1004978043895`, а `chat_link` — под id веб-клиента `-4978043895`; join шёл по СЫРЫМ id, поэтому такие чаты не находили ни одного сообщения → рассылка не определялась. Добавлена нормализация id (`normalizeTelegramId`/`telegramChatKey` в `src/lib/chat-list.ts`): снимаем знак и схлопываем префикс `100` супергруппы (с защитой по длине). Нормализуем ОБЕ стороны join в `src/lib/mailings-run.ts` и в SQL-функции (миграция `…_v10_…`). Также: сканируем подпись к медиа (`messageContent`), расширены глаголы долг-рассылки до стемов (напомн/уведом/сообщ/информир/рассыл/…), диагностика `chats_with_messages`/`unmatched_messages` и логи отличают «нет рассылки» от «синк не сработал». Зачем: пример-чат `-4978043895` и все супергруппы. Результат: ручная проверка — ключи веб-ссылки и Bot-API совпали, рассылка по долгу из подписи определяется.
  - Результат: `npm test` — 455 тестов зелёные (добавлены `confidence-calibration`, `match`, `confidence-evidence`, расширены `confidence-report`, `mailings-detect`, `chat-list`); `npm run build` зелёный. По итогам независимого ревью калиброванная уверенность тоже ограничена потолком 97% («никогда не 100%»). Статистика в отчёте считается по РЕАЛЬНЫМ данным БД; здесь приведены только результаты тестов (доступа к боевой БД из этой среды не было). Миграции: `20260717_mqa_detect_mailings_v10_chatid_normalization_debt_reminders.sql`.

## 2026-07-17 (2)
- Поиск чата стал терпимее к пунктуации/пробелам. Что: `matchesChatQuery` искал строгое вхождение всей строки, поэтому вставленное из таблицы название чуть отличалось от сохранённого («ИП Гончар Александр **/** 4345» vs «Александр**/** 4345», «ՍՊԸ Իմ Ջур, AM» vs «B-4809 «ՍՊԸ Իմ Ջуր», AM») → список был пуст, хотя чат есть. Добавлен «щадящий» режим: если каждое слово/число запроса встречается в данных чата (после нормализации `«» " , / . - :`) — совпадение засчитывается. Fast-path substring и матч по Telegram-ссылке сохранены. Зачем: Маргарита вставляла полные названия и получала «Нет чатов». Результат: `npm test` (399) и `tsc` зелёные; пастинг полного названия теперь находит чат.

## 2026-07-17
- Рассылка по ЗАРПЛАТЕ теперь подтягивается автоматически. Что: штатный шаблон «Сообщаем, что начисление ЗАРАБОТНОЙ ПЛАТЫ за текущий период не производится…» не содержит подстроки «зарплат», поэтому «словарный замок» рассылки (`зарплат|ведомост|зп|авансов`) не срабатывал → сигнал не возникал → статус оставался пустым и Маргарите приходилось ставить «Получил» вручную (чаты B-3576, B-3212, B-3371, B-3112, 992, 454). Добавлено слово «заработн» во все зарплатные правила в JS-детекторе (`src/lib/mailings-detect.ts`, `KW.salary_ru`) и в SQL-функции `mqa_detect_mailings` (миграция `20260717_…_v9…`, применена к боевой БД). Зачем: убрать ручную отметку рассылки по ЗП. Результат: проверено на реальных сообщениях всех 6 чатов — правило теперь срабатывает (было `false` → стало `true`); ручные отметки Маргариты не перезаписываются (guard `source <> 'manual'`).
- Рассылка по ДОЛГАМ ловится надёжнее. Что: напоминание обычно звучит «НАПОМИНАЕМ о задолженности», «ПРОСИМ/ПРОСЬБА оплатить», «ОПЛАТИТЕ до…», но в списке глаголов долгового `req` был только «напоминани» (не совпадает с «напоминаем») и не было «просим/просьба/оплатите/к оплате». Расширен набор глаголов в JS и SQL (v9). Зачем: долг-рассылка часто не подтягивалась. Результат: реальные формулировки теперь дают статус «1-й/2-й написал».
- Добавлены отсутствующие чаты для QA. Что: у чатов `-5248512177` («ИП Гончар Александр/ 4345 RU», бух. Թагуhi) и `B-4809` («ՍՊԸ Իմ Ջуր», бух. Դավիթ) строки были, но без `chat_link` — поэтому рассылки/активность не подтягивались; проставили ссылки и роли. Чаты `B-4805` («ООО Нева Сизонс Глобал Трэвел RU», бух. Դավիթ, `-5351039417`) и `В-4833` («ИП Манохина Вера RU», бух. Լիлиթ, `-5556488919`) отсутствовали — добавлены. Менеджер у всех — manager_onebusiness. Миграция `20260717_mqa_add_missing_qa_chats.sql` применена к боевой БД; чаты также вручную добавлены в «Активные за день» на 17.07, чтобы сразу были видны (у чатов нет своей активности — Telegram-бота нет в 3 из них). Зачем: «этих чатов нет в платформе». Результат: все 4 чата видны с реальными названиями, бухгалтерами и менеджером.
- Комментарий при добавлении нарушения теперь виден. Что: `ViolationModal` после сохранения показывал только «✓ Добавлено» и закрывался за 0.9 с — введённый комментарий нигде не отображался. Теперь подтверждение показывает сам комментарий, окно закрывается чуть позже (1.6 с). Само поле `note` сохранялось и раньше корректно (проверено в БД) — правился только показ. Зачем: жалоба «при добавлении нарушения комментарий не отображается». Результат: комментарий виден сразу после сохранения.
- «Апелляции»: чаты больше не показываются критичными без причины. Что: `listViolationAppealViews` не отфильтровывал мягко-удалённые авто-нарушения (`confirmed=false`, note='авто из оценки'), поэтому апелляция тянула снятое нарушение и чат показывался «Критичным», хотя нарушений нет и оценка 100. Добавлен фильтр `confirmed !== false` (как в `ViolationsPanel`/`appeals-report`/`work-report`). Зачем: бухгалтеры видели чат критичным, хотя он не критичный (авто-пометка ИИ). Результат: подтверждено 4 таких апелляции в БД — теперь они не показываются как критичные.
- PDF-отчёт: критичный чат больше не прячется за средним. Что: в таблице «Оценки всех бухгалтеров» показывалось только среднее (у Тагуи ≈100 при 1 критичном чате). Добавлена колонка «Крит.» (число критичных чатов за период, красная при >0): в `AccountantScore`/`DayAccountantScore` добавлено поле `critCount`, проброшено в сетку `report-pdf`. Зачем: критичный чат — самое серьёзное, его нельзя терять в среднем. Результат: рядом со средним теперь видно «Крит. 1»; `npm test` (399) и `tsc` зелёные.

## 2026-07-16
- Исправлено «[object Object]» при нажатии «Оценить». Что: (1) первопричина — фича «Уверенность AI» (PR #18) начала писать в `mqa_evaluations` колонки `ai_confidence`/`ai_total`/`review_status`/`reviewed_by`/`reviewed_at`, но миграция `20260716_mqa_evaluation_confidence.sql` не была применена к боевой БД, поэтому КАЖДОЕ сохранение оценки падало; миграция применена к боевому проекту — сохранение снова работает (перезапуск деплоя не требуется, чинилась схема БД, не код). (2) Почему на экране было «[object Object]»: Supabase/PostgREST бросают простой объект (`{ message, details, hint, code }`), а не `Error`, и `dbErrorResponse` делал `String(e)` → буквально «[object Object]», которое UI и показывал. Добавлен помощник `errorMessage()` (тянет `.message`/`.details`/`.hint` + код pg), `dbErrorResponse` использует его; маршруты `PUT`/`DELETE` в `evaluations/[id]` переведены на `dbErrorResponse` для единообразных сообщений и корректных кодов статуса. Зачем: Маргарита не могла сохранять оценки, а сообщение об ошибке было нечитаемым. Результат: `npm run build` успешен; типы изменённых файлов чисты; боевые колонки на месте (проверено в БД). Впредь любая ошибка БД читаема, а не опаковый «[object Object]».
- Учёт и аналитика уверенности модели («Уверенность AI»). Что: (1) МОДЕЛЬ — каждая AI-оценка чата теперь несёт уверенность модели в процентах (0..100), детерминированно вычисляемую в `ai.ts` (`predictionConfidence`: растёт с объёмом истории по бухгалтеру и числом обученных пар, падает при большой исторической ошибке `learned bias`; жёсткое правило «рассылка не выполнена» → высокая уверенность). Уверенность привязана к КОНКРЕТНОЙ версии прогноза (`AiPrediction.confidence` → `scores.ai.confidence`). (2) СОХРАННОСТЬ ИСХОДНИКА — таблица `mqa_evaluations` расширена колонками `ai_confidence`, `ai_total`, `review_status` (`not_reviewed`/`accepted`/`corrected`), `reviewed_by`, `reviewed_at` (миграция `20260716_mqa_evaluation_confidence.sql`, `db/schema.sql` обновлён, все поля nullable/с DEFAULT — существующие данные не ломаются). При ручной правке Маргаритой ОРИГИНАЛ AI (снимок `scores.ai` + `ai_confidence` + `ai_total`) НЕ перезаписывается (`deriveReviewFields` в `repo.ts` держит исходный снимок, финал пишется в остальные поля), фиксируются кто/когда правил; дубли записей проверки исключены существующим уникальным ключом (chat, day, role). (3) UI ОЦЕНКИ — в панели `ScoringPanel` рядом со строкой «🤖 AI» показывается цветная плашка уверенности, а строка «✍️ Вы» — статус проверки: «Принято без изменений» / «Исправлено Маргаритой» / «Не проверено». (4) ОТЧЁТ — новая страница `/confidence` («Уверенность AI» в навигации) + `ConfidenceReport.tsx` + API `/api/confidence-report`, движок `confidence-report.ts` (`buildConfidenceReport`): всего AI-оценок, принято/исправлено/не проверено (кол-во и %), разбивка по диапазонам 0–49/50–69/70–79/80–89/90–94/95–100 с процентом исправлений `corrected/reviewed×100` (непроверенные вне знаменателя), средняя уверенность принятых и исправленных, метрики ≥90% и выделенная «Точность оценок с уверенностью 90%+», корреляция Пирсона (уверенность ↔ факт исправления) только по проверенным с валидной уверенностью + предупреждение «Недостаточно данных…» при <30, русская интерпретация по знаку. Фильтры: сегодня/вчера/неделя/месяц/произвольный период (границы по времени Ереван, `confidence.ts`), бухгалтер, категория, диапазон уверенности, статус — пересчитывают все метрики/таблицы/графики. Два SVG-графика (процент исправлений по диапазонам; принято vs исправлено), состояния загрузки/пусто/ошибка. Строки без валидной уверенности показываются как «Нет данных» и исключены из расчётов по уверенности (не считаются 0%). Зачем: понять надёжность AI-оценок и проверить гипотезу «уверенность ≥90% ⇒ редкие правки». Результат: `npm test` — 399/399 (добавлены `confidence`, `confidence-report`, `confidence-repo`; обновлён `ai` под новое поле снимка), `npm run build` успешен, `next lint` без ошибок. Неразрушающая миграция; боевая БД не менялась (только SQL-файл для применения).
- Ежедневный отчёт бухгалтерии: динамика оценок всех бухгалтеров во вложении + компактное сообщение (отзыв руководителя). Что: (1) дневной PDF (`report-pdf.ts`, mode «daily») теперь рисует таблицу «Оценки всех бухгалтеров (динамика за период)» — по каждому бухгалтеру оценка за каждый день недели (окно ~7 дней до отчётного дня), колонка «Ср.» (среднее за период) и «Δ» (изменение: первый→последний день с оценкой), с цветными ячейками как в спреадшите; данные приходят отдельным многодневным `trend`-отчётом, который собирает `assemblePdfReport` (`report-data.ts`) и прокидывают обе точки входа PDF (`/api/report/pdf`, `/api/telegram/send`). Раньше во вложении была только строка за текущий день — динамики/прогресса не было. (2) Сообщение и дневной PDF больше не пишут «Нарушения: нет» под каждым бухгалтером: показываем ВСЕХ, но компактно — бухгалтеры без нарушений одним списком имён («Бухгалтеры без нарушений: Имя, Имя»), а нарушители детально («Бухгалтеры с нарушениями:» → имя → нарушения `код — тип — штраф`), плюс «Итого штрафов». Модель `buildDailyReportModel` даёт обе группы (`rows` — нарушители, `cleanAccountants` — без нарушений), поэтому сообщение и PDF по-прежнему из одного источника и не расходятся. Зачем: руководитель просил, чтобы каждый видел не только свой сегодняшний балл, но динамику и общий прогресс всех, а ежедневное сообщение стало компактнее и читаемее (без повторов «Нарушения: нет»). Результат: `npm test` — 372/372 (обновлены тесты формата сообщения, добавлен тест дневного PDF с trend-сеткой), `npx tsc --noEmit` по изменённым файлам чист; формат сообщения и сборка PDF проверены на сид-данных. Деньги считаются тем же движком `groupNarusheniya` (1-е нарушение за день — предупреждение/0 др, далее 1 000 др, ручная санкция перебивает) — цифры не менялись.
- Сквозная интеграция с приложением бухгалтеров (kk-accountants-feedback-form): реакции бухгалтера доходят до нашей платформы. Что: закреплён контракт нашей (принимающей) стороны для кросс-приложенческого цикла. Бухгалтеры НЕ работают в этом приложении — их UI это отдельное приложение, которое пишет «Ознакомлен»/«Подать апелляцию» напрямую в наши таблицы (`mqa_violations` / `mqa_violation_appeals`) через SECURITY DEFINER RPC (миграция на их стороне, `0027_kk_violation_workflow_bridge.sql`), проверяя владение по коду входа на стороне БД. Эти RPC создают ровно такую же форму строк, что и наши `acknowledgeViolation`/`createViolationAppeal` (переход `new→acknowledged`/`new→appealed`, апелляция с `accountant` нарушения, одна pending). Добавлен интеграционный тест `tests/accountant-integration.test.ts` (in-memory), который фиксирует обещание: как только такие строки появились, дневной отчёт Маргариты (`getViolationWorkflowReport`) и раздел «Работа Маргариты за день» для Telegram (`buildMargaritaWorkReportMessage`) их показывают (ознакомления, поданные/ожидающие апелляции, разбивка по бухгалтеру), а её решение (`resolveViolationAppeal`) возвращается на нарушение и в отчёт (принято/снят штраф); проверка владения не даёт чужому бухгалтеру попасть в отчёт. Зачем: раньше реакция бухгалтера жила только в их собственных `kk_*`-таблицах и наша платформа/отчёты/Telegram её не видели, а решение Маргариты не доходило до бухгалтера — цикл был разорван между приложениями. Мы разделяем один проект Supabase, поэтому связь сделана через общую БД (view + RPC на их стороне), без HTTP между приложениями (их приложение — статический SPA на anon-ключе, наши API закрыты для наших пользователей). Результат: `npm test` — 371/371 (добавлены 2 теста контракта, было 369), `npm run build` — успешно (`✓ Compiled successfully`). Наш код не менялся — только тест, фиксирующий уже существующее поведение принимающей стороны; миграция-мост живёт в репозитории бухгалтерского приложения и зависит от нашей `20260716_mqa_violation_workflow_appeals.sql`.
- Завершён рабочий цикл нарушений: «Ознакомлен» + апелляции, привязанные к нарушению, и отчётность по ним. Что: (1) модель — у `mqa_violations` появился единый статус рабочего цикла `status` (`new`/`acknowledged`/`appealed`/`appeal_approved`/`appeal_rejected`, Phase 11), поля `acknowledged_at`/`acknowledged_by`, а старое `appeal_status` держится синхронным ради обратной совместимости (дашборд/telegram/violation-report); новая таблица `mqa_violation_appeals` (FK на нарушение, `on delete cascade`) с частичным уникальным индексом «одна активная (pending) апелляция на нарушение» — миграция `20260716_mqa_violation_workflow_appeals.sql`, `db/schema.sql` обновлён, индексы под фильтры (status/accountant/vdate). (2) Логика — чистый модуль `violation-workflow.ts` (валидация текста апелляции, права/владение, допустимые переходы, идемпотентность) и агрегатор `appeals-report.ts` (`buildViolationWorkflowReport`: проверено чатов, создано/ознакомлено/не обработано нарушений, апелляции по статусам, безопасные проценты без деления на ноль, штрафы снятые одобрением, разбивка по бухгалтерам) — единый источник для web/telegram, покрыт тестами. (3) Репозиторий (Supabase + in-memory одинаково): `acknowledgeViolation` (идемпотентно, guard `status='new'`), `createViolationAppeal` (валидация + владение + «одна pending»), `resolveViolationAppeal` (атомарный guard `status='pending'`, повтор → 409), enrich-джойн для UI. (4) API: `POST /api/violations/[id]/acknowledge`, `POST /api/violations/[id]/appeal`, `GET /api/violation-appeals`, `PATCH /api/violation-appeals/[id]` — с аутентификацией, валидацией тела/владения/статуса, корректными HTTP-кодами (400/403/404/409) и без утечки стек-трейсов. (5) UI (RU, дизайн-система сохранена): в «Нарушениях» — статус и кнопки «Ознакомлен»/«Подать апелляцию» с формой и состоянием «Апелляция отправлена и ожидает решения Маргариты»; на `/appeals` — новый `ViolationAppealsPanel` с полной карточкой нарушения, фильтрами (статус/бухгалтер/даты) и решениями «Принять»/«Отклонить» с подтверждением (старый поток апелляций из формы бухгалтеров сохранён ниже); `/work-report` и `/dashboard` показывают метрики цикла из той же агрегации; в `/messages` добавлен раздел «Работа Маргариты за день» для Telegram (тот же агрегатор, plain-text без риска Markdown/HTML, при ошибке загрузки не шлёт ложные нули). (6) Fallback: сиды нарушений/апелляции для in-memory режима. Зачем: платформа должна доводить цикл до конца — бухгалтер знакомится или апеллирует, Маргарита принимает/отклоняет (и может сделать это сама), всё сохраняется и попадает в отчёты; ознакомления в системе не было вовсе. Результат: `npm test` — 369/369 (добавлены `violation-workflow`, `appeals-report`, `tasks-status`, `telegram-margarita`, `violation-appeals-repo`), `npm run build` успешен; end-to-end проверено на живом `next dev` (ознакомление идемпотентно; апелляция: пустой текст→400, чужое→403, владелец→201, второй pending→409; решение→200 и `appeal_approved`, повтор→409; страницы 200). Неразрушающая миграция, существующие данные сохраняются; boевая БД не менялась. Отдельно (Phase 7): чистый `tasks-status.ts` выводит канонический статус задачи (`new`/`in_progress`/`completed`/`overdue`/`cancelled`) и «просрочку» из даты дедлайна на чтении (выполненные/отменённые не просрочиваются) + видимость задач по бухгалтеру — без разрушительной миграции модели задач.
- Добавлены остальные «пропавшие» чаты (все 12 из фидбэка теперь в базе). Что: по присланным Маргаритой данным добавлены 9 недостающих чатов с № договора, именем, бухгалтером и ответственным менеджером: В-4801 (ИП Эльвира Эршлер, Լիлит), B-4869 (Երазанք Нахакртаран, Օлya), B-4868 (Бар Гаргулья, Արթур), B-4859 (Терраqор Сулюшнс — уже был в базе без ссылки, дозаполнены chat_link+manager, Դавит), B-4847 (Дмитрий Добрянский, Լилит), B-4832 (Сегарма, Դавит), B-4806 (Игорь Григорьев, Ռоберт), B-4774 (Car Hunter Sale, Օлya), B-4843 (Huameia Beauty, Դавит). Плюс проставлен ответственный менеджер (manager_onebusiness / +shogher) для 3 ранее добавленных (В-4216, B-3302, B-3609). Текст в SQL закодирован base64 и раскодирован через `convert_from` — гарантированно корректные армянские/русские байты. Миграция `20260716_mqa_add_remaining_chats.sql`. Зачем: Маргарита не видела эти чаты в дашборде. Результат: все 12 присланных чатов в `mqa_chats` с корректными бухгалтером и менеджером (проверено в БД); бухгалтеры сопоставлены с mqa_accountants. head accountant у всех — Эмилия (глобально, отдельного поля нет).
- Ответственные менеджеры по чатам (п.2): засеяны из мастер-книги. Что: заполнено поле `mqa_chats.manager` для 24 чатов на основе колонки Manager листа «Оценка». Взяты ТОЛЬКО действующие сотрудники (`isValidEmployee`), отличные от бухгалтера чата; уволенные/другие отделы/плейсхолдеры (Արտак 42, Սона 22, Աида 5, Լилит 2 14, Mane Lawer, Gohar Registration, Արам) сознательно НЕ засеяны — иначе в отчётах появились бы ушедшие сотрудники (ровно те «неточности», на которые жаловалась Маргарита). Заполнялись только пустые (`manager is null`) — ручные назначения через существующий редактор `/api/chats/manager` не перетёрты; ежедневный синк неразрушающий и manager не пишет, поэтому запись сохранится. Отображение уже было готово (менеджер виден в «Задачи — детали» и «Критичные чаты» дашборда и в дневном PDF). Миграция `20260716_mqa_seed_managers.sql`. Зачем: было заполнено лишь 7 из 708 чатов, поэтому «информации по менеджерам не было». Результат: 31 чат с менеджером, все 24 новых — действующие бухгалтеры (проверено в БД). Из 40 кандидатов 16 чатов отсутствуют в базе (номерные/иные № договора) — пропущены. Остальных менеджеров Маргарита проставит редактором.
- 9 «пропавших» чатов — НЕ решены новым файлом. Проверено: в присланном `One_Business_2.xlsx` нет ни одной Telegram-ссылки и ни одного из 9 chat_id; в бэкапах `mqa_backup_*` и в таблицах ингеста Telegram их тоже нет. Итог: восстановить № договора/имя/бухгалтера для этих 9 неоткуда — нужны данные от Маргариты (№ договора + имя + бухгалтер по каждой ссылке).
- Апелляции (п.4): подтверждение после решения. Что: после «Одобрить»/«Отклонить» показывается зелёное сообщение «Апелляция … одобрена/отклонена — она ушла из списка „На рассмотрении“, смотрите в фильтре „Одобрены/Отклонены“ или „Все“», с крестиком закрытия. Зачем: при фильтре по умолчанию «На рассмотрении» решённая апелляция сразу исчезает из списка (корректно — она больше не pending), но без явного сообщения это выглядит как «кнопка ничего не сделала»/«апелляция пропала». Сама логика решения не менялась (PATCH `/api/appeals/[id]` работает). Результат: `tsc` чисто, `next build` успешен. Примечание: сообщение Маргариты «невозможно нажать на кнопки» пока не воспроизведено — ждём видео, чтобы понять, это ли устраняет проблему или дело в оверлее/вёрстке на её устройстве.
- Дневной PDF: добавлена общая таблица оценок всех бухгалтеров (п.1) + таблица менеджеров (п.2). Что: в дневном PDF-отчёте (`renderDaily`) перед «Звездой дня» теперь рисуется таблица «Оценки бухгалтеров» — колонки Бухгалтер · Оценка % · Низких (Плохо/Критично) · Оценено N, с цветом ячейки % как в спреадшите; если менеджер отвечал в чатах и есть оценки, отдельной таблицей выводятся «Оценки менеджеров» (иначе опускается). Раньше дневной PDF показывал только прозу дневного сообщения (сервис %, звезда, запросы, нарушения) без сводной таблицы оценок. Зачем: жалоба Маргариты — «в PDF нет общей таблицы с оценками всех бухгалтеров, а текст дублирует сообщение». Результат: `tsc` чисто, тесты 335/335, `next build` успешен, дневной PDF рендерится (valid `%PDF`) с новой таблицей. Логика цифр не менялась — источник тот же (`report.perAccountant`/`managerScores`), поэтому таблица совпадает с дашбордом.
- История оценок за неделю на дашборде (п.6) + добавлены отсутствующие чаты. Что: (1) новый раздел «История оценок за неделю» на `/dashboard` — полная таблица «бухгалтер × день» (Пн…Вс) со средней оценкой за каждый день, столбцом «Неделя» и строкой «Среднее по отделу», навигация ◀ Пред. / След. ▶ по неделям (текущая и предыдущие); данные из того же движка `buildReport` (многодневное окно → `perDayPerAccountant`/`perDay`), что и недельный PDF/сообщение, поэтому экран и выгрузка совпадают. Реализовано без клиентского JS (навигация — обычные ссылки `?week=<понедельник>`), сохраняет фильтры по бухгалтеру/клиенту. (2) Из 12 присланных Telegram-ссылок «нет в дашборде» 3 восстановлены из мастер-таблицы и добавлены в `mqa_chats` (миграция `20260716_mqa_add_missing_chats.sql`): В-4216 (ИП Алена Туманова, Գայանե, Active), B-3609 (Shaju Group LLC, Ստելլա, Active), B-3302 (ИП Маргарита Даниелян, Գայանե — в источнике Inactive, добавлен как Inactive). Зачем: Маргарита не видела полную недельную таблицу оценок и не могла найти/добавить эти чаты. Результат: `tsc` чисто, `next build` успешен, `/dashboard` остаётся динамическим; 3 чата теперь в базе (проверено). Остальные 9 ссылок в присланных выгрузках отсутствуют — нужны № договора / имя / бухгалтер, чтобы добавить корректно (не создаём пустышки).
- Дашборд («Отчёт») больше не требует нескольких перезагрузок для правильных цифр. Что: (1) `AutoRefresh` теперь делает `router.refresh()` СРАЗУ при монтировании (раньше — только по таймеру 45 с и по фокусу), а in-flight-флаг привязан к реальному завершению перехода, а не к таймеру 800 мс; (2) в `next.config.mjs` задан `experimental.staleTimes = { dynamic: 0, static: 0 }`, чтобы динамические страницы всегда дотягивали свежие данные при навигации, а не переиспользовали устаревшую копию из клиентского Router Cache Next.js; (3) профилактика того же класса — `listTasks` получил детерминированный порядок (`checking_date desc`) и явный `.limit(20000)`, `listScoreOverrides` — `.limit(20000)`, как остальные запросы в `repo.ts`. Зачем: жалоба «корректная информация в Dashboard появляется только после нескольких обновлений страницы». Данные на сервере полные и детерминированные (проверено на боевом «OB FAQ»: все 6532 оценки отдаются под лимитом, серверного кэпа строк нет — `db_max_rows=null`), поэтому причина была на клиенте: первый экран показывал устаревшую пре-фетч копию из Router Cache, а `AutoRefresh` обновлял её только через 45 с. Результат: `tsc` чисто, `next build` успешен, `/dashboard` остаётся динамическим (`ƒ server-rendered on demand`); при заходе/навигации показываются свежие данные без ручного обновления. Боевая БД не менялась (только чтение при диагностике).

## 2026-07-15
- Ежедневный отчёт на `/messages` больше не показывает «0% / без звезды дня» до первой оценки за день. Что: убран жёсткий override окна на «сегодня» в `MessagesPage` — по умолчанию окно снова резолвит `getDailyAnalytics` (последний ОЦЕНЁННЫЙ день: `max(checking_date)`). Если Маргарита уже оценила чаты сегодня — это сегодня; если за сегодня ещё нет ни одной оценки, показывается последний день с оценками (напр. вчера), а не пустой отчёт. Явные даты фильтра по-прежнему в приоритете. Зачем: жалоба — на боевом «Общий уровень сервиса: 0% по отделу» и нет «Звезды дня», хотя «Кол-во запросов за день» живое; причина — запросы берутся из Telegram-данных (есть за сегодня), а сервис %/звёзды — из оценок (за сегодня их ещё не было), поэтому привязка отчёта к «сегодня» давала 0%. Результат: `tsc` чисто, `next lint` без ошибок, тесты отчёта 42/42. На боевых данных «OB FAQ» отчёт теперь резолвит 14.07 (сервис 97.8%, звёзды дня — Ստելլա/Հասմիկ/Մարիաննա/Լիլիթ Ք․ по 100%) вместо пустого 15.07. Боевая БД не менялась (только чтение).
- Задача из «Оценки» с полным набором полей + фикс «лишних» нарушений в «Отчёте по работе». Что: (1) модалка добавления задачи на странице «Оценка» (`TaskModal`) теперь несёт те же поля, что и таблица «Задачи»: № / Чат / Бухгалтер, Описание, Due Date (Original), Due Date (Postponed), Completed At, Приоритет, Статус (`SINGLE_TASK_STATUSES`) и Состояние (открыта/закрыта, авто-вывод через `isTaskClosed`); при статусе «Completed …» дата выполнения проставляется автоматически. Раньше можно было задать только срок/приоритет/бухгалтера/описание, полноценно завести задачу — только на странице «Задачи». (2) В `buildWorkReport` («Отчёт по работе») нарушения теперь считаются только по подтверждённым записям (`confirmed !== false`) — авто/легаси-строки («авто из оценки», `confirmed=false`) исключены, как в дашборде, ежедневном отчёте и разбивке нарушений. Зачем: «в отчёте по работе слишком много ненастоящих нарушений» и «в Оценке при добавлении задачи не хватает колонок». Результат: `tsc` чисто, тесты 335/335 (добавлен тест на исключение неподтверждённых нарушений), `next build` успешен. Боевая БД не менялась.
- Отчёты «День»/«Неделя» и совпадение PDF ↔ сообщение. Что: (1) удалён блок «🗂 Сверка налогового кабинета» целиком — карточка на `/messages`, её расчёт, функция `buildTaxCabinetReconciliation` и типы `TaxCabinetRow`/`TaxCabinetReconInput`, оба теста. (2) PDF теперь ДВУХ видов через параметр `?period=`: `daily` — строго за один день (портрет: «Общий уровень сервиса», «Звезда дня», «Кол-во запросов за день» с нарушениями под каждым бухгалтером) и `weekly` — таблица мониторинга по дням (ландшафт, Пн→день). Раньше «дневной» PDF молча расширялся до недели — отсюда рассинхрон с сообщением. (3) Ежедневное сообщение и дневной PDF считаются из ОДНОЙ модели `buildDailyReportModel` (сервис %, звёзды, запросы, нарушения по единому движку `groupNarusheniya`), поэтому цифры всегда совпадают. Кнопки «📄 PDF (день)» и «📄 PDF (неделя)» на соответствующих карточках; отправка в Telegram прикладывает нужный PDF (`pdf.period`). Зачем: жалоба «PDF и сообщения не совпадают», нужен раздельный дневной/недельный отчёт, лишний блок сверки убрать. Результат: `tsc` чисто, тесты 334/334, `next build` успешен; дневной и недельный PDF рендерятся (valid `%PDF`), дневной PDF показывает те же данные, что и сообщение. Боевая БД не менялась.
- Сверка чатов проведена на РЕАЛЬНЫХ данных + ESLint для CI. Что: новый скрипт `scripts/reconcile-live.ts` сверяет авторитетный Excel-«Чаты» с живым реестром `mqa_chats` (переиспользует протестированный `chat-reconcile.ts`, читает БД из снапшота `--db-json` или напрямую из Supabase), npm-скрипт `reconcile:live`; добавлен неинтерактивный `.eslintrc.json` (`next/core-web-vitals` + `@typescript-eslint/no-unused-vars`), убраны найденные линтером неиспользуемые переменные. Зачем: файл-2 требовал реальную сверку и доставочные цифры (раньше в репозитории не было ни одного .xlsx, сверка не запускалась), а `next lint` без конфига падал интерактивно и не работал в CI. Результат (прод-проект «OB FAQ», 705 чатов / 691 активный): 686 совпало, 0 активных чатов пропущено, 0 контрактов без ссылки, 17 расхождений привязки на ручную проверку (в основном один чат на несколько № договора). Из 9 названных «пропавших» чатов: 3 уже присутствуют и привязаны (B-4852, B-4821, `-5212778373` как заглушка `TG-5212778373`), 2 неактивны в источнике и корректно исключены (N-3292, B-3302), 4 отсутствуют и в источнике, и в базе. `npm run lint` — 0 ошибок, `tsc` чисто, тесты 336/336, build успешен. Продовая БД не менялась (сверка только на чтение).

## 2026-07-14
- Заведён журнал изменений, коммит-gate (`doc-sync`) и защита ветки `main`. Зачем: изменение кода не должно попадать в продукт без следа «что поменяли». Результат: работа только через PR, мердж на красном физически заблокирован для всех.
- Убрана QA-концепция «Без ответа» полностью: страница `/unanswered` → редирект на `/dashboard`, ссылка из навигации, секции в отчёте/PDF/сообщениях, поля `unansweredChats` в модели отчёта, API `/api/unanswered/*`, `lib/unanswered.ts` и связанные функции в `repo.ts`, тесты. Зачем: функциональность признана лишней и путала бухгалтеров. Результат: исходные Telegram-сообщения в БД не тронуты, отчёты/PDF/сообщения больше не считают и не показывают «Без ответа»; поле `mqa_chats.unanswered` сохранено (используется только для порядка списка на странице оценки).
- Удалена страница «Регистрация» (`/registration`, еженедельный журнал QA менеджеров) → редирект на `/dashboard`; убраны `RegistrationPanel`, API `/api/manager-evaluations`, функции журнала в `repo.ts`, типы `ManagerEvaluation`/`NewManagerEvaluationInput` и поле стора. Зачем: страница выведена из эксплуатации. Результат: схема оценки `registration` в «Регламенте» (калькуляторы) сохранена — трогали только журнал.
- Убран чекбокс «Повторяющаяся задача» при создании задачи (страница «Задачи» и модалка на «Оценке»): из UI, состояния формы, payload. Зачем: по запросу. Результат: создание/закрытие задач работает; колонка «Повтор»/«QA подтвердил» в таблице задач убрана (в проде задач ещё не было).
- Полностью удалено автоматическое создание нарушений («Авто из оценки»): убран авто-POST нарушения при оценке «Критично» (`ScoringPanel`), функция `importCriticalChatsAsViolations` и маршрут `/api/violations/import-critical`, кнопка «Импортировать критичные чаты» и бейджи «Авто-импорт/не подтв.». Зачем: нарушения вносит только Маргарита вручную. Результат: скоринг больше не создаёт нарушений; ручные нарушения и их история сохранены.
- Сверка чатов (файл-2, критический приоритет). Чистый движок `chat-reconcile.ts`: `telegramChatIdOf`/`normalizeTelegramLink` (сохранение отрицательных ID, «Telegram»-заглушка → null), `normalizeClientName` (AM/RU/EN, ИП/ԱՁ/ООО/LLC, кавычки, N-теги, порядок слов), `reconcileChats` (сопоставление по chat-id → № договора → точному нормализованному имени; классы matched/missing/link_missing/conflict — неоднозначное НЕ сливается, а помечается на разбор), `computeChatHealth` (реестр: без договора/бухгалтера/ссылки, дубли привязки). Админ-страница `/reconcile` + ссылка «Сверка» показывает эти пробелы сразу. Импортированы заведомо отсутствующие активные чаты B-4852 (−5392224018, AM) и B-4821 (−5413997128, RU) с сохранением отрицательных ID (миграция `20260714_mqa_import_missing_active_chats.sql`, применена). B-4794/−5212778373 — реальный конфликт (контракт без ссылки + отдельная TG-заглушка с историей): не сливается автоматически, выведен на разбор. Установлено: реестр `mqa_chats` (688 активных, 678 с ID) полнее Excel-«Чаты» (642 активных с ID); перечисленные «пропавшие» чаты отсутствовали и в Excel. Тесты `chat-reconcile.test.ts` (нормализация, сопоставление, дедуп, здоровье реестра).
- Новый отчёт соответствия рассылок (файл-2): страница `/mailing-report` + ссылка «Рассылки» в навигации. Чистый агрегатор `mailing-compliance.ts` (`buildMailingCompliance`) считает УНИКАЛЬНЫЕ применимые чаты по бухгалтеру и категории (До 28 — Первичная документация, Долги до 5, До 10 — Заработная плата, До 15 — Основные налоги): ровно один статус на чат/категорию/цикл, приоритет ручного подтверждения Маргариты над авто, неактивные исключены, сумма статусов ≤ числу применимых чатов, один чат может быть в разных категориях (отдельные потоки), но в каждой — один раз. Каждый счётчик раскрывается до точного списка чатов (клиент/договор/ссылка/статус/подтверждение). Telegram-версия `buildMailingComplianceMessage` (формат файла-2: скрытие нулевых статусов/пустых бухгалтеров, период сверху) — тот же канонический объект, что и UI. Тесты `mailing-compliance.test.ts` (уникальность, override, исключение неактивных, дедуп, drill-down, формат сообщения). Данные — живые `mqa_chat_mailings` (за 202607 — 1882 строки). Ограничение: правка статуса прямо из этого отчёта пока не встроена — Маргарита подтверждает статусы в панели «Оценка», и отчёт их сразу отражает.
- Недельная история нарушений в «Сообщениях» (п.10): новая функция `buildWeeklyViolationHistory` — группировка по дням, только дни с нарушениями, по каждому чату одна строка (типы через запятую), бухгалтер, предупреждение/штраф с суммой и комментарий; неделя выводится из выбранной даты отчёта (Ереван). Итог недели считается тем же движком `groupNarusheniya`, что дашборд/PDF, поэтому совпадает; учитываются только подтверждённые нарушения. Добавлена карточка на странице `/messages`. Тест сверяет группировку/дедуп/итог.
- Живое обновление без перезагрузки (п.8 / п.4 / п.6 / файл-2): компонент `AutoRefresh` (контролируемый поллинг + обновление по фокусу через `router.refresh()`, один интервал, очистка слушателей, пропуск при скрытой вкладке/при активном обновлении, индикатор «Обновление данных…»). Подключён к `/work-report`, `/appeals` и `/dashboard` (кроме режима сохранённого снимка). Работает и с Supabase, и с in-memory. Так как страницы — `force-dynamic` server components с каноническим `buildReport`, правка данных (в т.ч. за прошлый день) подхватывается автоматически на следующем тике/фокусе.
- Исправлен счётчик «Запрос» в ежедневном сообщении (файл-2): считались все сообщения клиента (отсюда невозможные «Запрос — 60»). Теперь единица — уникальный чат: `tallyRequestChats` считает уникальные `chat_id` на бухгалтера (5 сообщений в одном чате = 1 запрос, дубли синка не раздувают, чужие чаты исключены), деление на дни убрано. Тесты `request-count.test.ts`.
- Единый источник расчётов для сообщений/PDF/дашборда (п.7): `buildReportMessage` теперь считает штрафы тем же движком `groupNarusheniya`/`computeViolationFines`, что и PDF и дашборд (раньше был свой инлайн-расчёт, игнорировавший ручные санкции и группировку по чату). Ручная санкция перебивает авто-правило и попадает в сообщение так же, как в PDF; тяжесть сама по себе сумму не раздувает. Тесты консистентности `report-consistency.test.ts` сверяют итоги штрафов и строки «один чат — одна строка» между сообщением, PDF-движком и дашбордом.
- Тикеты формы обратной связи бухгалтеров (`kk_accountant_feedback`, 22 шт.) теперь появляются как апелляции в `/appeals` (п.4). `listAppeals` объединяет нативные апелляции (`kk_problem_appeals`) и тикеты формы (маппинг ситуация/решение → комментарий, статус «pending», источник сохранён), дедуп по `source_id`; решение Маргариты сохраняется бэкфиллом строки в `kk_problem_appeals` (upsert по `source_id`, зеркалится в `kk_problems`). Счётчики (total/pending/approved/rejected) на дашборде и в «Отчёте по работе» учитывают эти апелляции. Малформед-тикеты (без `problem_id`) логируются и показываются, а не теряются. Миграция `20260714_mqa_appeals_from_accountant_feedback.sql` (поля `source`/`source_id` + уникальный индекс) применена к боевой БД. Проверено на реальных данных: 22 тикета выводятся как pending-апелляции с привязкой к клиенту.
- Под «📌 Сохранено» в панели оценки (п.2) выводится сводка распознанных рассылок чата: «Найдено: Налоги, Первичка», «Дата: 09.07.2026» (дата подтверждения/сообщения, не «сегодня»), статус «Подтверждено Маргаритой» либо «Авто-распознавание»; если ничего не найдено — «Рассылки не обнаружены». Дата берётся из `confirmed_at ?? detected_at` рассылки.
- Определение рассылок (п.1) значительно усилено. `mailings-detect.ts`: добавлены английские правила (налоги/зп/первичка/долги, done/req/paid/call/neg), правило «налоговой рассылки» (перечень налогов + расчётные счета → «Отправил» без явного глагола), исправлено армянское определение запроса («խնդրում» → стем `խնդր`) и снят ложный done на «տրամադրել» в запросах, добавлены `ինվոյս/ֆակտուր/CMR/մաքս/ներմուծ` в первичку, детектор онбординг-шаблона `isOnboardingMessage`. Новый модуль `mailings-classify.ts`: структурированный `classifyMessage` (категория, статус, тип, уверенность, доказательства, источник) + самообучение — фингерпринты из подтверждённых примеров Маргариты с порогом минимума примеров (safeguard) и приоритетом над правилами; онбординг никогда не считается выполненной рассылкой. Тест-сюит `mailings-classify.test.ts` покрывает реальные примеры (RU-налоги, HY первичка/зп-запрос, EN, смешанные, мультикатегория, онбординг, долги, ложные срабатывания, обучение/коррекции/пороги).
- Отображение нарушений приведено к требованиям: один чат = одна строка (несколько нарушений по чату — через запятую, движок `groupNarusheniya`); сотрудники без нарушений не выводятся (п.9), общий пустой статус «Нет нарушений за выбранный период»; в расчёты попадают только подтверждённые ручные нарушения (`confirmed !== false`) — в дашборде, сообщениях и журнале. Миграция `20260714_mqa_exclude_auto_from_score_violations.sql` пометила 38 исторических авто-нарушений (`note='авто из оценки'`) как `confirmed=false` (исключены из расчётов, строки не удалены). Применена к боевой БД. Осталось 64 ручных нарушения.
